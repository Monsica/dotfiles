- defaults:
    link:
      relink: true

- clean: ['~']

#- link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
#    ~/.vimrc: vimrc

- create:
    - ~/Dev

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
