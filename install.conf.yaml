- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig:
    ~/.config:

- create:
    - ~/Dev

- shell:
  - command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
