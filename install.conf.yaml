- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config:

- create:
    - ~/Dev

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
